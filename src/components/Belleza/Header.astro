<header class="w-full flex justify-center items-center p-6 bg-gradient-to-r from-cherry via-pink-50 to-cherry relative">
  <div class="aspect-[16/9] w-[100%] md:w-[70%] lg:w-[35%] relative">
    <img 
      class="w-full h-full rounded-xl shadow-lg animate__animated animate__fadeIn animate__slow object-cover"
      src="/img/bg.webp" 
      alt="fondo">
  </div>

  <h1 class="absolute text-center text-pink font-bold text-2xl md:text-4xl drop-shadow-2xl animate__animated animate__fadeInUp animate__delay-1s bg-[var(--pink-shadow)] px-2 rounded-r-full rounded-l-full">
    Encuentra tu brillo
  </h1>
</header>

<style>
  header img {
    transition: transform 0.6s ease, box-shadow 0.6s ease;
  }
  header img:hover {
    transform: scale(1.05);
    box-shadow: 0 12px 25px var(--pink-shadow);
  }
</style>